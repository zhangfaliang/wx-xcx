<view class="item" bindtap="navigateToDetail">
    <image src="{{imgUrl}}" class="img"></image>
    <view class="left">
      <view class="title">{{title}}</view>
      <view class="detail"></view>
    </view>
</view>