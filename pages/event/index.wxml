<view class="container">
  event
  <text id="tapTest" data-hi="WeChat" bindtap="tapName">Click me!</text>

  <view id="view" bind:tap='handleTap1'>
    outer view
    <view id="middle" catch:tap="handleTap2">
      middle view
      <view id="inner" bind:tap="handleTap3">
        inner view
      </view>
    </view>
  </view>
  <view id="outer" bind:touchstart="touchstart1" capture-bind:touchstart="capture1">
    outer view
    <view id="inner" bind:touchstart="touchstart2" capture-bind:touchstart="captuer2" >
      inner view
    </view>
  </view>

    <view id="outer" bind:touchstart="touchstart1" capture-catch:touchstart="capture1">
    outer view
    <view id="inner" bind:touchstart="touchstart2" capture-catch:touchstart="captuer2" >
      inner view
    </view>
  </view>
</view>