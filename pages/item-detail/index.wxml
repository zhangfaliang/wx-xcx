<view wx:for="{{pageData}}" wx:key="{{item.index}}" class="detail">
    <ad unit-id="adunit-4f50cb67110611d4"></ad>
    {{item.content}}
    <image wx:if="{{!!item.imgUrl}}" src="{{item.imgUrl}}" class="img"></image>
</view>